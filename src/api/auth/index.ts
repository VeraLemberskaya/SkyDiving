import {
  useGenerateCredentialsMutation,
  useLoginMutation,
  useUserInfoQuery,
} from './auth.query';

export const auth = {
  useGenerateCredentialsMutation,
  useLoginMutation,
  useUserInfoQuery,
};
