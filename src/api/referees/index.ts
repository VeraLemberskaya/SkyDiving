import {
  useRefereesQuery,
  useCompetitionRefereesQuery,
  useDeleteRefereeFromCompetitionMutation,
} from './referees.query';

export const referees = {
  useRefereesQuery,
  useCompetitionRefereesQuery,
  useDeleteRefereeFromCompetitionMutation,
};
