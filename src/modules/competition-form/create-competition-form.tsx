import { CompetitionForm } from './competition-form';

export const CreateCompetitionForm = () => {
  const handleSubmit = () => {
    //create
  };

  return <CompetitionForm onSubmit={handleSubmit} />;
};
